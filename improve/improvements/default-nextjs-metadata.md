---
schemaVersion: 1
id: default-nextjs-metadata
priority: P2
lens: product
status: open
title: App ships the default "Create Next App" title and description to production
summary: layout.tsx still carries the create-next-app boilerplate metadata, so the browser tab, SEO, and social embeds all read "Create Next App" / "Generated by create next app"
fix: Set metadata.title/description in layout.tsx to the real product ("Research Helper") (~2 min)
leverage: { impact: high, effort: low }
blast_radius: [routing-app-shell]
cites:
  - src/app/layout.tsx:18 :: Create Next App
  - src/app/layout.tsx:19 :: Generated by create next app
found: 2026-07-20
---
`layout.tsx` — the only real Server Component and the one place metadata is set
([nextjs-app-shell-convention](/brain/rules/nextjs-app-shell-convention.md)) — still exports the
unmodified create-next-app scaffold: `title: "Create Next App"` (`layout.tsx:18`) and
`description: "Generated by create next app"` (`layout.tsx:19`). The UI header already says
"Research Helper" (`Main.tsx:27`), so the document title contradicts the visible product. This
ships to the browser tab, search-engine results, and link-preview cards on every deploy. No gate
flags it because it is valid. Canonical fill-while-you're-here fix.
