# Allowlist the LangGraph deployment host to close the LANGSMITH_API_KEY exfiltration gap (lgc-allowlist)

* [Allowlist LGC_DEPLOYMENT_URL's host in isSafeDeploymentUrl; encode lgcDeploymentUrl in page.tsx](lgc-allowlist/lgc-allowlist-fix.md) - isSafeDeploymentUrl (route.ts:59-84) is deny-private only, so https://attacker.example passes and receives langsmithApiKey. Add an allowlist check against LGC_DEPLOYMENT_URL's host alongside the existing DNS resolution and private-IP checks (never replacing them). While in this path, wrap lgcDeploymentUrl in encodeURIComponent at page.tsx:24 (unencoded-lgc-deployment-url) so a `&`/`#` in the value can't inject or truncate query parameters.
* [Allowlist the LangGraph deployment host to close the LANGSMITH_API_KEY exfiltration gap](lgc-allowlist/lgc-allowlist.md) - isSafeDeploymentUrl (route.ts:59-84) only denies private/internal targets, so any public host a caller names in ?lgcDeploymentUrl= still passes and receives langsmithApiKey — open P1 lgc-deployment-url-key-exfiltration. This is the residual the DNS-resolution SSRF fix (lgc-deployment-url-ssrf-asymmetry, P2, already resolved) did not close: deny-private is not allow-known.
